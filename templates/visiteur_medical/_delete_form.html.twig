<form method="post" action="{{ path('app_visiteur_medical_delete', {'id': visiteur_medical.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ visiteur_medical.id) }}">
    <button class="btn">Delete</button>
</form>
