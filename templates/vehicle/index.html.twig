{% extends 'base.html.twig' %}

{% block title %}Gestion des Véhicules{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="text-center">Liste des Véhicules 🚗</h1>

    <table class="table table-striped mt-4">
        <thead class="thead-dark">
            <tr>
                <th>Marque</th>
                <th>Modèle</th>
                <th>Immatriculation</th>
                <th>Statut</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for vehicle in vehicles %}
                <tr>
                    <td>{{ vehicle.marque }}</td>
                    <td>{{ vehicle.modele }}</td>
                    <td>{{ vehicle.immatriculation }}</td>
                    <td>
                        {% if vehicle.statut == 'disponible' %}
                            <span class="badge badge-success">Disponible</span>
                        {% elseif vehicle.statut == 'prete' %}
                            <span class="badge badge-warning">Prêté</span>
                        {% else %}
                            <span class="badge badge-danger">En entretien</span>
                        {% endif %}
                    </td>
                    <td>
                        <a href="{{ path('app_vehicle_edit', {'id': vehicle.id}) }}" class="btn btn-primary btn-sm">Modifier</a>
                        <a href="{{ path('app_vehicle_delete', {'id': vehicle.id}) }}" class="btn btn-danger btn-sm" onclick="return confirm('Voulez-vous vraiment supprimer ce véhicule ?');">Supprimer</a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="5" class="text-center">Aucun véhicule enregistré.</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_vehicle_add') }}" class="btn btn-success">Ajouter un véhicule</a>
</div>
{% endblock %}
