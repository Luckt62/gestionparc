{% extends 'base.html.twig' %}

{% block title %}Nouveau Véhicule{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="display-4 text-center mb-4">Créer un Nouveau Véhicule</h1>

        {# Formulaire de création d'un véhicule avec un formulaire inclus #}
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Détails du Véhicule</h5>
                {{ include('vehicule/_form.html.twig') }}
            </div>
        </div>

        <div class="d-flex justify-content-between mt-4">
            <a href="{{ path('app_vehicule_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>
    </div>
{% endblock %}
