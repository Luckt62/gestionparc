<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Accueil{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
    <!-- Navigation Bar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
        <div class="container">
            <a class="navbar-brand" href="#">GSB - Véhicules</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <!-- Lien de connexion -->
                        {% if path('app_login', {}, false) is not null %}
                            <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                        {% endif %}
                        {% if path('app_register', {}, false) is not null %}
                            <a class="nav-link" href="{{ path('app_register') }}">S'inscrire</a>
                        {% endif %}
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Hero Section (Bienvenue) -->
    <section class="hero-section">
        <div class="container mt-5 pt-5">
            <h1 class="display-4 text-center">Bienvenue sur notre site de gestion de Véhicules</h1>
            <p class="lead text-center">Nous sommes ravis de vous accueillir.</p>
        </div>
    </section>

    <!-- Footer -->
    <footer class="footer text-center mt-5 bg-dark text-white py-3">
        <p>&copy; 2025 GSB - Tous droits réservés</p>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
